<button type="{{ $type }}" class="btn ml-auto" onclick="{{ $onClick }}">
    <p class="h5 d-inline">
        <img src="{{ $image }}" width="20" height="20" class="d-inline-block" title="{{ $title }}" alt="{{ $alt }}">
        {{ $label }}
    </p>
</button>
